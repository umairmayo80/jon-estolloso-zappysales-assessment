# Submission artifact

The final self-contained source is
`Tariq_Sardar_Umair_AssessmentForFullStackDeveloper_2026-09-03.md`.
It contains the setup, verification, capture, design, and PDF commands needed
for review.

Render the PDF and page previews from the repository root:

```bash
scripts/render-submission-pdf.sh --check-tools
scripts/render-submission-pdf.sh
```

The generated PDF is written to `output/pdf/`; its rendered page previews are
written beneath `output/pdf/previews/`. Submit the PDF rather than this source
file, and exclude `.env`, keys, tokens, and real personal data.
